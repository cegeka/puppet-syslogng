log { source(<%= @source %>); <% if @parser -%>parser(<%= @parser %>); <% end -%><% if @filter -%>filter(<%= @filter %>); <% end -%><% if @rewrite -%>rewrite(<%= @rewrite %>); <% end -%>destination(<%= @destination %>); <% unless @flags.empty? -%>flags(<%= Array(@flags).join(', ') %>); <% end -%>};
